{
  "name": "tacap",
  "version": "1.0.0",
  "description": "Aplicativo para registro de desempenho em avaliação física de capoeira",
  "main": "electron/main.ts",
  "scripts": {
    "dev:vite": "vite --port 5173",
    "dev:electron": "tsc -p tsconfig.node.json --watch & wait-on http://localhost:5173 && electron electron/main.ts",
    "dev": "npx concurrently \"npm run dev:vite\" \"npm run dev:electron\"",
    "build:vite": "vite build",
    "build:electron": "tsc -p tsconfig.node.json && electron-builder",
    "build": "npm run build:vite && npm run build:electron",
    "start": "dist_electron/\"TACAP Setup 1.0.0.exe\""
  }
,
  "private": true,
  "author": "",
  "license": "ISC",
  "build": {
    "appId": "com.seuapp.tacap",
    "productName": "TACAP",
    "directories": {
      "output": "dist_electron"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "node_modules/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "icon.ico",
      "forceCodeSigning": false
    }
  },
  "devDependencies": {
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "concurrently": "^9.2.1",
    "electron": "^38.1.2",
    "vite": "^7.1.5",
    "wait-on": "^9.0.0",
    "electron-builder": "^24.6.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.3",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
